# Frontend runtime/API configuration
# Set when frontend and backend are on different origins.
VITE_API_BASE_URL=http://localhost:8000

# Optional: dev server proxy target. Falls back to VITE_API_BASE_URL when unset.
VITE_PROXY_TARGET=http://localhost:8000
